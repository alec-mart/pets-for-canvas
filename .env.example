# backend (Railway env vars) and tools/inbox.py
DATABASE_URL=postgresql://...        # unset = local SQLite at backend/canvas_digest.db
CORS_ORIGINS=*
DEV_SECRET=change-me                 # x-dev header for admin endpoints and dev-copy tier forcing
